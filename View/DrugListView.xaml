<UserControl x:Class="PharmacyApp.View.DrugListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:PharmacyApp.View"
             mc:Ignorable="d" >
    <StackPanel Background="{StaticResource BGColor}">
        <Border Style="{StaticResource StyleBorder}"
                Padding="5">
            <TextBlock>
                  <Run Text="Name:" 
                       FontWeight="SemiBold"/> 
                  <Run Text="{Binding Path=Name}"/>
                  <LineBreak/>
                  <Run Text="Category:"
                       FontWeight="SemiBold"/>
                  <Run Text="{Binding Path=Category.CategoryName}"/>
            </TextBlock>
        </Border>
    </StackPanel>
</UserControl>
